import { StyleSheet } from '@react-pdf/renderer';

export const styles = StyleSheet.create({
    summary: {
        fontSize: '20px',
        lineHeight: '1.5',
        marginBottom: '32px'
    }
});
